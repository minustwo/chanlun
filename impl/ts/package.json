{
  "name": "chanlun-impl-ts",
  "version": "1.0.0",
  "private": true,
  "description": "Faithful TypeScript port of the chanlun-v1 reference backend - byte-equivalent to the Python oracle against the FROZEN 48-fixture corpus.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "check": "tsc && node dist/check.js"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "@types/node": "20.11.30"
  },
  "engines": {
    "node": ">=20"
  }
}
